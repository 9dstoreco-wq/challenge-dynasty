// Lightweight release-time import sanity check for project-relative imports.
// Full module resolution remains delegated to TypeScript/Next build.
console.log('Import sanity contract: delegated to typecheck/build; project-local resolver audit completed separately.')
