'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { submitSkillChallenge } from '@/app/actions/skills'
import { useTranslations } from 'next-intl'
export default function SkillSubmissionForm({challengeId}:{challengeId:string}){const t=useTranslations('Tricks');const router=useRouter();const [url,setUrl]=useState('');const [caption,setCaption]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await submitSkillChallenge({challengeId,videoUrl:url,caption});router.refresh();setUrl('');setCaption('')}catch(e){setError(e instanceof Error?e.message:t('errSubmitFailed'))}finally{setBusy(false)}}return <form onSubmit={submit} className="mt-6 rounded-2xl bg-white/5 border border-white/10 p-4 space-y-3"><div className="text-xs tracking-[.2em] text-white/40 font-black">{t('uploadTag')}</div><input required type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://..." className="w-full rounded-xl bg-[#0A0A0C] border border-white/10 px-4 py-3"/><input value={caption} onChange={e=>setCaption(e.target.value)} placeholder={t('captionPlaceholder')} className="w-full rounded-xl bg-[#0A0A0C] border border-white/10 px-4 py-3"/>{error&&<div className="text-sm text-red-300">{error}</div>}<button disabled={busy} className="rounded-xl bg-white text-black px-4 py-3 font-black">{busy?t('sending'):t('sendProof')}</button></form>}
